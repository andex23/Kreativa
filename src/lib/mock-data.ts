// Real Nigerian Creatives Data
// Profiles will be added via admin bulk import or user submissions

import { Profile } from './types';

// Start with empty profiles - add real ones via Admin → Bulk Import
export const mockProfiles: Profile[] = [];
